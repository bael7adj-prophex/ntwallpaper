<UserControl x:Class="NTWallpaper.Presentation.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NTWallpaper.Presentation.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:SettingsViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <ScrollViewer Margin="24">
        <StackPanel MaxWidth="700" HorizontalAlignment="Left">
            <TextBlock Text="Settings" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,16" />

            <Border Style="{StaticResource CardBorder}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Pixabay API key" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,8" />
                    <TextBlock Text="Stored encrypted with DPAPI. Get a free key at pixabay.com." Foreground="{DynamicResource ForegroundMutedBrush}" FontSize="11" Margin="0,0,0,8" />
                    <PasswordBox x:Name="ApiKeyBox" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorder}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Startup" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,8" />
                    <CheckBox Content="Start with Windows (no admin)" IsChecked="{Binding StartWithWindows}" Foreground="{DynamicResource ForegroundBrush}" />
                    <CheckBox Content="Start minimized to system tray" IsChecked="{Binding StartMinimized}" Foreground="{DynamicResource ForegroundBrush}" Margin="0,4,0,0" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorder}" Margin="0,0,0,12">
                <StackPanel>
                    <TextBlock Text="Appearance" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,8" />
                    <TextBlock Text="Theme" Foreground="{DynamicResource ForegroundMutedBrush}" FontSize="11" />
                    <ComboBox ItemsSource="{Binding ThemeOptions}" SelectedItem="{Binding Theme}" Width="200" HorizontalAlignment="Left" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorder}">
                <StackPanel>
                    <TextBlock Text="Network" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,8" />
                    <TextBlock Text="Retries on transient failures (3 recommended)" Foreground="{DynamicResource ForegroundMutedBrush}" FontSize="11" />
                    <TextBox Text="{Binding RetryCount}" Width="80" HorizontalAlignment="Left" />
                </StackPanel>
            </Border>

            <TextBlock Text="{Binding StatusMessage}" Foreground="{DynamicResource ForegroundMutedBrush}" FontSize="11" Margin="0,12,0,0" />
        </StackPanel>
    </ScrollViewer>
</UserControl>
