<UserControl x:Class="NTWallpaper.Presentation.Views.GroupsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:NTWallpaper.Presentation.ViewModels"
             d:DataContext="{d:DesignInstance Type=vm:GroupsViewModel}"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Text="Groups &amp; Targets" FontSize="28" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,16" />

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Style="{StaticResource CardBorder}" Margin="0,0,8,0">
                <StackPanel>
                    <TextBlock Text="Create new group" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,8" />
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBox Text="{Binding NewGroupName, UpdateSourceTrigger=PropertyChanged}" />
                        <ComboBox Grid.Column="1" ItemsSource="{Binding RotationIntervals}" SelectedItem="{Binding NewGroupInterval}" Width="120" Margin="8,0,0,0" />
                    </Grid>
                    <Button Content="Create" Style="{StaticResource PrimaryButton}" Command="{Binding CreateGroupCommand}" HorizontalAlignment="Right" Margin="0,12,0,0" />

                    <Separator Margin="0,16" Background="{DynamicResource BorderBrush}" />
                    <TextBlock Text="Existing groups" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,8" />
                    <ListBox ItemsSource="{Binding Groups}" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource ForegroundBrush}" MaxHeight="400">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,4">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" />
                                        <TextBlock Text="{Binding RotationInterval}" Foreground="{DynamicResource ForegroundMutedBrush}" FontSize="11" />
                                    </StackPanel>
                                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                                        <Button Content="Toggle" Style="{StaticResource SecondaryButton}" Margin="0,0,4,0"
                                                Command="{Binding DataContext.ToggleEnabledCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                CommandParameter="{Binding}" />
                                        <Button Content="Delete" Style="{StaticResource SecondaryButton}"
                                                Command="{Binding DataContext.DeleteGroupCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                                CommandParameter="{Binding}" />
                                    </StackPanel>
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource CardBorder}" Grid.Column="1" Margin="8,0,0,0">
                <StackPanel>
                    <TextBlock Text="Discovered targets" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,8" />
                    <ListBox ItemsSource="{Binding Targets}" Background="Transparent" BorderThickness="0" Foreground="{DynamicResource ForegroundBrush}" MaxHeight="400">
                        <ListBox.ItemTemplate>
                            <DataTemplate>
                                <Grid Margin="0,2">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding DisplayName}" Foreground="{DynamicResource ForegroundBrush}" />
                                        <TextBlock Text="{Binding Id}" Foreground="{DynamicResource ForegroundMutedBrush}" FontSize="11" />
                                    </StackPanel>
                                    <Button Content="Unassign" Style="{StaticResource SecondaryButton}"
                                            Command="{Binding DataContext.UnassignTargetCommand, RelativeSource={RelativeSource AncestorType=ListBox}}"
                                            CommandParameter="{Binding}" />
                                </Grid>
                            </DataTemplate>
                        </ListBox.ItemTemplate>
                    </ListBox>

                    <Separator Margin="0,16" Background="{DynamicResource BorderBrush}" />
                    <TextBlock Text="Tags" FontWeight="SemiBold" Foreground="{DynamicResource ForegroundBrush}" Margin="0,0,0,8" />
                    <ItemsControl ItemsSource="{Binding Tags}">
                        <ItemsControl.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel />
                            </ItemsPanelTemplate>
                        </ItemsControl.ItemsPanel>
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Border Background="{DynamicResource AccentBrush}" CornerRadius="12" Padding="10,4" Margin="0,0,6,6">
                                    <TextBlock Text="{Binding Name}" Foreground="White" />
                                </Border>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</UserControl>
